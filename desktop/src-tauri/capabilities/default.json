{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the main Teak window.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
